version: '3'

services:

  rstudio:

    build: 
      context: .
      dockerfile: Dockerfile

    environment:
      ROOT: 'TRUE'
      # PASSWORD: 'asdfasdf'

    volumes:
      - ..:/home/rstudio/work:cached